query getLinkSkills($memberId: Int!, $skill: String!) {
  linkSkills(memberId: $memberId, skill: $skill) {
    category
    category_name
    effect
    skill_name
    is_act2
    members {
      id
      name
    }
  }
}
