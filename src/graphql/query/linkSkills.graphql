query getLinkSkills($member: String!, $skill: String!) {
  linkSkills(member: $member, skill: $skill) {
    category
    category_name
    effect
    skill_name
    is_act2
    members {
      id
      name
    }
  }
}
